import { FC } from 'react';
import Map from '../ui/Map';

const RouterDistribution: FC = () => {
    return (
        <div className='w-full h-[25vw]'>
            <Map />
        </div>
    );
};

export default RouterDistribution;